# Server
PORT=5000
CLIENT_URL=http://localhost:5173

# MongoDB
MONGO_URI=mongodb://localhost:27017/leadhub

# Auth (used only to protect YOUR dashboard login, not IndiaMART)
JWT_SECRET=change_this_to_a_long_random_string

# NOTE: IndiaMART API key, SMTP credentials, WhatsApp session and Gemini API key
# are NOT stored here. Every user enters their own credentials from the
# in-app Settings panel after logging in, and they are saved encrypted-at-rest
# in MongoDB per user. This file only holds infra-level config.
